import java.security.PublicKey;

public class Ferry extends Ship {
    private String FerryNumber;
    private boolean mailingList;

    public Ferry(String name, String build, String crew) {
        super(name, build, crew);
    }

    public Ferry() {
        Public Ferry(String name, String built, String crew boolean mailingList) {
            super(name,built,crew);
            this.FerryNumber = FerryNumber;
            this.mailingList = mailingList;
        }

    }
}
